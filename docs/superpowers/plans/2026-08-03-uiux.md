# CODE UI/UX Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mengganti 33 dialog bawaan browser dengan umpan balik yang menyatu dengan CODE, memperbaiki status kosong dan memuat, memasang badge notifikasi, dan menghapus `framer-motion` (114,5 KB).

**Architecture:** Dua provider di akar aplikasi menyediakan toast dan modal konfirmasi lewat hook, sehingga pemanggil tetap sesederhana bentuk aslinya. Sebuah uji penjaga mengunci batasan teks dengan pola ratchet: daftar pengecualian dimulai dari pelanggaran yang sudah ada dan menyusut tiap task sampai kosong, sehingga suite hijau sepanjang jalan namun tidak ada pelanggaran baru yang bisa masuk.

**Tech Stack:** React 18, Tailwind CSS, `lucide-react` (sudah ada). Tidak ada dependensi baru.

## Global Constraints

Dari `docs/superpowers/specs/2026-08-03-uiux-design.md`, ditetapkan pemilik sistem:

- **Palet tetap.** Hanya `cimoryBlue` (#1B3A6F), `cimoryRed` (#E63946), `cimoryLight` (#F8FAFC), `cimoryGray` (#E5E7EB). Tidak ada warna baru di `tailwind.config.js`.
- **Tata letak dan posisi tetap.** Sidebar kiri, header atas, kartu dashboard vertikal, tombol CODE AI pojok kanan bawah.
- **Jumlah klik tidak bertambah** untuk alur mana pun.
- **Tidak ada emoji sebagai ikon.** Pakai `lucide-react`. Ikonnya harus **cocok maknanya**, bukan tukar mekanis: `"✅"` yang menandakan "URL terisi" menjadi ikon yang berarti tautan atau tercentang, bukan sembarang ikon.
- **Tidak ada em dash (—) di teks yang dibaca user.** Ganti koma, titik dua, atau kalimat terpisah. Nilai kosong ditulis teks biasa atau tanda hubung.
- Dua batasan terakhir berlaku untuk teks antarmuka. Pesan `console.*` yang sudah memuat emoji dibiarkan; kode baru tetap mengikuti batasan.
- **Tidak ada dependensi npm baru.** Bundle halaman login baru diturunkan 816 KB ke 242,8 KB.
- Branch kerja: buat `feat/uiux` dari `fix/audit-findings`.
- Setelah mengubah `src/`, jalankan `cd frontend && npx vite build` sebelum menyatakan selesai. Build Vite **tidak** menangkap komponen yang dipakai tapi belum diimpor; itu meledak saat dijalankan sebagai `X is not defined`.

---

### Task 1: Uji penjaga batasan teks

Dibuat lebih dulu supaya seluruh task berikutnya punya penjaga. Memakai pola ratchet: pelanggaran yang sudah ada didaftarkan sebagai pengecualian, dan tiap task mencabut entri yang sudah diperbaiki.

**Files:**
- Create: `frontend/tests/text-constraints.test.mjs`
- Modify: `frontend/package.json` (script `test` menjalankan dua berkas)

**Interfaces:**
- Produces: `npm test` di `frontend/` gagal bila ada emoji, em dash, `alert(`, atau `window.confirm(` baru di `src/`.

- [ ] **Step 1: Tulis uji penjaga**

Buat `frontend/tests/text-constraints.test.mjs`:

```js
// Menjaga batasan teks yang ditetapkan pemilik sistem: tidak ada emoji sebagai
// ikon, tidak ada em dash di teks yang dibaca user, dan tidak ada dialog
// bawaan browser.
//
// Pola ratchet: SISA_PELANGGARAN berisi yang sudah ada saat batasan ini
// ditetapkan. Angkanya hanya boleh turun. Uji ini gagal bila ada pelanggaran
// BARU, dan juga gagal bila angkanya lebih kecil dari kenyataan (berarti
// daftarnya basi dan harus diturunkan).
import fs from "fs";
import path from "path";

const SRC = "src";

// Diturunkan tiap task. Nol saat Task 10 selesai.
const SISA_PELANGGARAN = {
  emojiUi: 2,      // DashboardManager.jsx:315 dan ManageUsers.jsx:56
  emDashUi: 6,     // 5 di string literal, 1 di teks JSX
  alert: 27,
  confirm: 6,
};

// Rentang emoji yang lazim dipakai orang sebagai ikon.
const EMOJI = /[\u{1F300}-\u{1FAFF}\u{2190}-\u{21FF}\u{2600}-\u{27BF}\u{2B00}-\u{2BFF}\u{FE0F}\u{2705}\u{274C}\u{26A0}]/u;

function berkasJsx(dir, keluar = []) {
  for (const nama of fs.readdirSync(dir)) {
    const p = path.join(dir, nama);
    if (fs.statSync(p).isDirectory()) berkasJsx(p, keluar);
    else if (/\.(jsx?|mjs)$/.test(nama)) keluar.push(p);
  }
  return keluar;
}

/**
 * Baris yang tampil ke user: string literal dan teks JSX. Komentar dan
 * console.* dikecualikan — keduanya untuk pengembang, bukan antarmuka.
 */
function barisAntarmuka(isi) {
  const hasil = [];
  const baris = isi.split(/\r?\n/);
  let dalamBlokKomentar = false;

  for (let i = 0; i < baris.length; i += 1) {
    let b = baris[i];

    if (dalamBlokKomentar) {
      if (b.includes("*/")) { dalamBlokKomentar = false; b = b.slice(b.indexOf("*/") + 2); }
      else continue;
    }
    if (b.includes("/*") && !b.includes("*/")) { dalamBlokKomentar = true; b = b.slice(0, b.indexOf("/*")); }

    b = b.replace(/\/\/.*$/, "").replace(/\/\*.*?\*\//g, "");
    if (/console\.(log|warn|error|info|debug)/.test(b)) continue;
    if (!b.trim()) continue;

    hasil.push({ nomor: i + 1, teks: b });
  }
  return hasil;
}

const temuan = { emojiUi: [], emDashUi: [], alert: [], confirm: [] };

for (const berkas of berkasJsx(SRC)) {
  const isi = fs.readFileSync(berkas, "utf8");

  for (const { nomor, teks } of barisAntarmuka(isi)) {
    if (EMOJI.test(teks)) temuan.emojiUi.push(`${berkas}:${nomor}`);
    if (teks.includes("—")) temuan.emDashUi.push(`${berkas}:${nomor}`);
    if (/\balert\(/.test(teks)) temuan.alert.push(`${berkas}:${nomor}`);
    if (/window\.confirm\(/.test(teks)) temuan.confirm.push(`${berkas}:${nomor}`);
  }
}

let lulus = 0;
let gagal = 0;

function periksa(kunci, judul) {
  const ada = temuan[kunci].length;
  const batas = SISA_PELANGGARAN[kunci];

  if (ada > batas) {
    gagal += 1;
    console.log(`  FAIL  ${judul}: ${ada} ditemukan, batas ${batas}`);
    for (const lokasi of temuan[kunci]) console.log(`          ${lokasi}`);
    return;
  }
  if (ada < batas) {
    gagal += 1;
    console.log(`  FAIL  ${judul}: tinggal ${ada}, tapi batas masih ${batas}. Turunkan SISA_PELANGGARAN.${kunci} menjadi ${ada}.`);
    return;
  }
  lulus += 1;
  console.log(`  PASS  ${judul}: ${ada} (sesuai batas)`);
}

periksa("emojiUi", "emoji di teks antarmuka");
periksa("emDashUi", "em dash di teks antarmuka");
periksa("alert", "pemanggilan alert()");
periksa("confirm", "pemanggilan window.confirm()");

console.log(`\n──────── ${lulus} passed, ${gagal} failed ────────\n`);
process.exit(gagal === 0 ? 0 : 1);
```

- [ ] **Step 2: Jalankan, lihat angka sebenarnya**

Run: `cd frontend && node tests/text-constraints.test.mjs`

Kemungkinan besar angkanya tidak persis sama dengan tebakan di `SISA_PELANGGARAN`, karena definisi "baris antarmuka" di sini lebih ketat daripada `grep` biasa. **Sesuaikan `SISA_PELANGGARAN` ke angka yang dilaporkan uji ini**, bukan sebaliknya. Jangan melemahkan pola `EMOJI` atau `barisAntarmuka` agar angkanya cocok.

Expected setelah disesuaikan: `4 passed, 0 failed`

- [ ] **Step 3: Buktikan penjaga menangkap pelanggaran baru**

Tambahkan sementara ke `src/components/PerfSummary.jsx`, di dalam JSX-nya:

```jsx
      <p>uji penjaga: ✅ dan em dash — di sini</p>
```

Run: `cd frontend && node tests/text-constraints.test.mjs`
Expected: `FAIL emoji di teks antarmuka` dan `FAIL em dash di teks antarmuka`, menyebut `src/components/PerfSummary.jsx` beserta nomor barisnya.

Hapus baris percobaan itu, jalankan lagi, pastikan kembali `4 passed`.

- [ ] **Step 4: Daftarkan di npm test**

Di `frontend/package.json`, ubah script `test`:

```json
    "test": "node tests/sanitize.test.mjs && node tests/text-constraints.test.mjs"
```

Run: `cd frontend && npm test`
Expected: 18 passed dari sanitize, lalu 4 passed dari text-constraints.

- [ ] **Step 5: Commit**

```bash
git add frontend/tests/text-constraints.test.mjs frontend/package.json
git commit -m "test(ui): fail the suite on new emoji, em dash, alert, or confirm"
```

---

### Task 2: ToastProvider

**Files:**
- Create: `frontend/src/components/ToastProvider.jsx`
- Modify: `frontend/src/main.jsx`
- Modify: `frontend/tailwind.config.js` (keyframes masuk dan keluar)

**Interfaces:**
- Produces:
  - `<ToastProvider>{children}</ToastProvider>` default export dari berkas yang sama
  - `useToast()` mengembalikan `{ success, error, warning, info }`
  - Setiap fungsi bertanda tangan `(pesan: string, opsi?: { durasiMs?: number }) => void`

- [ ] **Step 1: Tambahkan keyframes ke Tailwind**

Di `frontend/tailwind.config.js`, tambahkan ke `extend.animation` dan `extend.keyframes` yang sudah ada. Jangan mengganti isinya, hanya menambah:

```js
      animation: {
        "gradient-slow": "gradientBG 10s ease infinite",
        "float-slow": "float 15s ease-in-out infinite",
        "float-fast": "float 8s ease-in-out infinite",
        // Toast dan panel: masuk dari kanan, keluar memudar.
        "toast-in": "toastIn 180ms ease-out",
        "toast-out": "toastOut 150ms ease-in forwards",
        // Kelas animate-fade-in sudah dipakai di App.jsx tetapi keyframes-nya
        // tidak pernah didefinisikan, jadi selama ini tidak berefek apa pun.
        "fade-in": "fadeIn 200ms ease-out",
      },
      keyframes: {
        gradientBG: {
          "0%, 100%": { backgroundPosition: "0% 50%" },
          "50%": { backgroundPosition: "100% 50%" },
        },
        float: {
          "0%, 100%": { transform: "translateY(0px)" },
          "50%": { transform: "translateY(-20px)" },
        },
        toastIn: {
          from: { opacity: "0", transform: "translateX(16px)" },
          to:   { opacity: "1", transform: "translateX(0)" },
        },
        toastOut: {
          from: { opacity: "1", transform: "translateX(0)" },
          to:   { opacity: "0", transform: "translateX(16px)" },
        },
        fadeIn: {
          from: { opacity: "0" },
          to:   { opacity: "1" },
        },
      },
```

- [ ] **Step 2: Tulis ToastProvider**

Buat `frontend/src/components/ToastProvider.jsx`:

```jsx
// ─────────────────────────────────────────────────────────────────────────────
// Umpan balik yang tidak memblokir, pengganti alert().
//
// Dibuat sendiri, bukan memakai library. Bundle halaman login baru diturunkan
// dari 816 KB ke 242,8 KB; menambah dependensi untuk komponen sekecil ini akan
// membatalkan sebagian usaha itu. Buatan sendiri juga memakai warna Cimory
// langsung tanpa menimpa gaya bawaan library.
// ─────────────────────────────────────────────────────────────────────────────

import { createContext, useCallback, useContext, useMemo, useRef, useState } from "react";
import { CheckCircle2, XCircle, AlertTriangle, Info, X } from "lucide-react";

const ToastContext = createContext(null);

const MAKS_TAMPIL = 4;      // lebih dari ini menutupi layar
const DURASI_BAWAAN = 4000;
const DURASI_ERROR = 7000;  // pesan gagal perlu waktu untuk dibaca

const GAYA = {
  success: { Ikon: CheckCircle2,   kelas: "border-l-4 border-green-600",     warnaIkon: "text-green-600" },
  error:   { Ikon: XCircle,        kelas: "border-l-4 border-cimoryRed",     warnaIkon: "text-cimoryRed" },
  warning: { Ikon: AlertTriangle,  kelas: "border-l-4 border-amber-500",     warnaIkon: "text-amber-500" },
  info:    { Ikon: Info,           kelas: "border-l-4 border-cimoryBlue",    warnaIkon: "text-cimoryBlue" },
};

export function useToast() {
  const ctx = useContext(ToastContext);
  if (!ctx) throw new Error("useToast dipakai di luar ToastProvider");
  return ctx;
}

export default function ToastProvider({ children }) {
  const [daftar, setDaftar] = useState([]);
  const berikutnya = useRef(1);

  const tutup = useCallback((id) => {
    setDaftar((sebelum) => sebelum.filter((t) => t.id !== id));
  }, []);

  const tambah = useCallback((jenis, pesan, opsi = {}) => {
    if (!pesan) return;
    const id = berikutnya.current;
    berikutnya.current += 1;

    const durasiMs = opsi.durasiMs ?? (jenis === "error" ? DURASI_ERROR : DURASI_BAWAAN);

    setDaftar((sebelum) => {
      // Yang tertua digeser keluar supaya tumpukan tidak menutupi layar.
      const dipangkas = sebelum.slice(-(MAKS_TAMPIL - 1));
      return [...dipangkas, { id, jenis, pesan: String(pesan) }];
    });

    setTimeout(() => tutup(id), durasiMs);
  }, [tutup]);

  const api = useMemo(() => ({
    success: (pesan, opsi) => tambah("success", pesan, opsi),
    error:   (pesan, opsi) => tambah("error", pesan, opsi),
    warning: (pesan, opsi) => tambah("warning", pesan, opsi),
    info:    (pesan, opsi) => tambah("info", pesan, opsi),
  }), [tambah]);

  return (
    <ToastContext.Provider value={api}>
      {children}

      {/* Di bawah header, di atas segalanya. pointer-events-none pada wadah
          supaya area kosongnya tidak menghalangi klik ke halaman. */}
      <div
        className="fixed top-20 right-4 z-[100] flex flex-col gap-2 w-[min(22rem,calc(100vw-2rem))] pointer-events-none"
        aria-live="polite"
        aria-atomic="false"
      >
        {daftar.map(({ id, jenis, pesan }) => {
          const { Ikon, kelas, warnaIkon } = GAYA[jenis] || GAYA.info;
          return (
            <div
              key={id}
              role={jenis === "error" ? "alert" : "status"}
              className={`pointer-events-auto flex items-start gap-2.5 bg-white ${kelas} rounded-lg shadow-lg px-3.5 py-3 animate-toast-in`}
            >
              <Ikon size={18} className={`${warnaIkon} shrink-0 mt-0.5`} />
              <p className="text-sm text-gray-800 flex-1 break-words">{pesan}</p>
              <button
                onClick={() => tutup(id)}
                className="shrink-0 text-gray-400 hover:text-gray-700 transition rounded"
                aria-label="Tutup pemberitahuan"
              >
                <X size={16} />
              </button>
            </div>
          );
        })}
      </div>
    </ToastContext.Provider>
  );
}
```

- [ ] **Step 3: Pasang di akar aplikasi**

Di `frontend/src/main.jsx`, bungkus `<App />`. Provider berada di **dalam** `BrowserRouter` supaya toast bisa dipicu dari komponen mana pun yang memakai router:

```jsx
import React from "react";
import ReactDOM from "react-dom/client";
import { BrowserRouter } from "react-router-dom";
import App from "./App";
import ToastProvider from "./components/ToastProvider";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BrowserRouter>
      <ToastProvider>
        <App />
      </ToastProvider>
    </BrowserRouter>
  </React.StrictMode>
);
```

`ToastProvider` diimpor statis, bukan lazy: ia harus siap sebelum komponen pertama memanggil `useToast()`, dan ukurannya di bawah 3 KB.

- [ ] **Step 4: Verifikasi build dan impor**

Run:

```bash
cd frontend && npx vite build 2>&1 | grep -E "✓ built|error"
```

Expected: `✓ built`

Lalu pemeriksa impor, karena build Vite tidak menangkap komponen yang dipakai tapi belum diimpor:

```bash
cd frontend && node -e "
const fs=require('fs');
for (const f of ['src/components/ToastProvider.jsx','src/main.jsx']) {
  const src=fs.readFileSync(f,'utf8');
  const bound=new Set();
  for(const m of src.matchAll(/^\s*import\s+([^]*?)\s+from\s*['\"][^'\"]*['\"]/gm))
    for(const n of m[1].matchAll(/([A-Za-z_\$][\w\$]*)/g)) bound.add(n[1]);
  for(const m of src.matchAll(/(?:function|const|let|class)\s+([A-Z][\w\$]*)/g)) bound.add(m[1]);
  for(const m of src.matchAll(/[:{,(]\s*([A-Z][\w\$]*)\s*(?=[,}=):])/g)) bound.add(m[1]);
  const used=new Set([...src.matchAll(/<([A-Z][\w\$]*)[\s/>]/g)].map(m=>m[1]));
  const missing=[...used].filter(n=>!bound.has(n));
  console.log((missing.length?'MISSING ':'OK      ')+f+(missing.length?': '+missing.join(', '):''));
}"
```

Expected: `OK` untuk keduanya.

- [ ] **Step 5: Verifikasi manual empat varian**

Tambahkan sementara ke `src/components/PerfSummary.jsx` di dalam komponennya:

```jsx
  const toast = useToast();
```

dan sebuah tombol sementara di dalam JSX-nya:

```jsx
  <button onClick={() => { toast.success("Berhasil disimpan"); toast.error("Gagal menyimpan"); toast.warning("Isi dulu semua kolom"); toast.info("Kuota AI tersisa 40 persen"); }}>uji toast</button>
```

Tambahkan importnya: `import { useToast } from "./ToastProvider";`

Jalankan `npm run dev`, login sebagai admin, buka Manage Users, klik tombol itu. Periksa dengan mata:

1. Empat toast muncul bertumpuk di kanan atas, tidak menutupi header
2. Halaman **tetap bisa diklik** selagi toast tampil, tidak seperti `alert()`
3. Toast sukses, peringatan, dan info hilang sendiri setelah 4 detik; yang error bertahan lebih lama
4. Tombol X menutup toast satu per satu
5. Memicu 6 toast sekaligus hanya menampilkan 4

Hapus seluruh kode percobaan itu setelah selesai.

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/ToastProvider.jsx frontend/src/main.jsx frontend/tailwind.config.js
git commit -m "feat(ui): add non-blocking toasts to replace browser alerts"
```

---

### Task 3: ConfirmProvider

**Files:**
- Create: `frontend/src/components/ConfirmProvider.jsx`
- Modify: `frontend/src/main.jsx`

**Interfaces:**
- Consumes: keyframes `animate-fade-in` dari Task 2
- Produces:
  - `<ConfirmProvider>{children}</ConfirmProvider>` default export
  - `useConfirm()` mengembalikan `(opsi) => Promise<boolean>`
  - `opsi` berbentuk `{ judul: string, pesan: string, labelKonfirmasi?: string, destruktif?: boolean }`

- [ ] **Step 1: Tulis ConfirmProvider**

Buat `frontend/src/components/ConfirmProvider.jsx`:

```jsx
// ─────────────────────────────────────────────────────────────────────────────
// Pengganti window.confirm() yang bisa distyle dan bisa diuji.
//
// Mengembalikan promise supaya kode pemanggil tetap sesederhana bentuk aslinya:
//   if (!(await confirm({ ... }))) return;
// ─────────────────────────────────────────────────────────────────────────────

import { createContext, useCallback, useContext, useEffect, useRef, useState } from "react";
import { AlertTriangle } from "lucide-react";

const ConfirmContext = createContext(null);

export function useConfirm() {
  const ctx = useContext(ConfirmContext);
  if (!ctx) throw new Error("useConfirm dipakai di luar ConfirmProvider");
  return ctx;
}

export default function ConfirmProvider({ children }) {
  const [permintaan, setPermintaan] = useState(null);
  const penyelesai = useRef(null);
  const tombolAmanRef = useRef(null);
  const dialogRef = useRef(null);

  const confirm = useCallback((opsi) => {
    return new Promise((resolve) => {
      penyelesai.current = resolve;
      setPermintaan({
        judul: opsi?.judul || "Konfirmasi",
        pesan: opsi?.pesan || "",
        labelKonfirmasi: opsi?.labelKonfirmasi || "Lanjutkan",
        destruktif: Boolean(opsi?.destruktif),
      });
    });
  }, []);

  const jawab = useCallback((nilai) => {
    setPermintaan(null);
    const resolve = penyelesai.current;
    penyelesai.current = null;
    if (resolve) resolve(nilai);
  }, []);

  // Esc membatalkan. Fokus awal jatuh ke tombol AMAN, bukan tombol hapus:
  // menekan Enter secara refleks tidak boleh menghapus apa pun.
  //
  // Tab juga diperangkap di dalam modal. Tanpa itu fokus bisa berpindah ke
  // halaman di belakang, sehingga pemakai keyboard bisa menekan tombol yang
  // tertutup lapisan gelap dan tidak ia lihat.
  useEffect(() => {
    if (!permintaan) return;
    tombolAmanRef.current?.focus();

    const onKey = (e) => {
      if (e.key === "Escape") { e.preventDefault(); jawab(false); return; }
      if (e.key !== "Tab") return;

      const fokusable = dialogRef.current?.querySelectorAll("button");
      if (!fokusable || fokusable.length === 0) return;
      const pertama = fokusable[0];
      const terakhir = fokusable[fokusable.length - 1];

      if (e.shiftKey && document.activeElement === pertama) {
        e.preventDefault();
        terakhir.focus();
      } else if (!e.shiftKey && document.activeElement === terakhir) {
        e.preventDefault();
        pertama.focus();
      }
    };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, [permintaan, jawab]);

  return (
    <ConfirmContext.Provider value={confirm}>
      {children}

      {permintaan && (
        <div
          className="fixed inset-0 z-[110] bg-black/50 flex items-center justify-center px-4 animate-fade-in"
          role="dialog"
          aria-modal="true"
          aria-labelledby="konfirmasi-judul"
        >
          <div ref={dialogRef} className="bg-white rounded-xl shadow-2xl w-full max-w-md p-5">
            <div className="flex items-start gap-3">
              <AlertTriangle
                size={22}
                className={permintaan.destruktif ? "text-cimoryRed shrink-0 mt-0.5" : "text-cimoryBlue shrink-0 mt-0.5"}
              />
              <div className="flex-1">
                <h2 id="konfirmasi-judul" className="text-base font-semibold text-cimoryBlue">
                  {permintaan.judul}
                </h2>
                {permintaan.pesan && (
                  <p className="text-sm text-gray-700 mt-1.5">{permintaan.pesan}</p>
                )}
              </div>
            </div>

            <div className="flex justify-end gap-2 mt-5">
              <button
                ref={tombolAmanRef}
                onClick={() => jawab(false)}
                className="px-4 py-2 rounded-lg border hover:bg-gray-100 transition text-sm"
              >
                Batal
              </button>
              <button
                onClick={() => jawab(true)}
                className={`px-4 py-2 rounded-lg text-white transition text-sm ${
                  permintaan.destruktif
                    ? "bg-cimoryRed hover:bg-red-700"
                    : "bg-cimoryBlue hover:bg-blue-800"
                }`}
              >
                {permintaan.labelKonfirmasi}
              </button>
            </div>
          </div>
        </div>
      )}
    </ConfirmContext.Provider>
  );
}
```

- [ ] **Step 2: Pasang di akar, di dalam ToastProvider**

Di `frontend/src/main.jsx`:

```jsx
import React from "react";
import ReactDOM from "react-dom/client";
import { BrowserRouter } from "react-router-dom";
import App from "./App";
import ToastProvider from "./components/ToastProvider";
import ConfirmProvider from "./components/ConfirmProvider";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BrowserRouter>
      <ToastProvider>
        <ConfirmProvider>
          <App />
        </ConfirmProvider>
      </ToastProvider>
    </BrowserRouter>
  </React.StrictMode>
);
```

- [ ] **Step 3: Verifikasi build**

Run: `cd frontend && npx vite build 2>&1 | grep -E "✓ built|error"`
Expected: `✓ built`

- [ ] **Step 4: Verifikasi manual**

Pakai tombol percobaan sementara seperti pada Task 2 Step 5, kali ini:

```jsx
  const confirm = useConfirm();
```

```jsx
  <button onClick={async () => { const ya = await confirm({ judul: "Hapus dashboard", pesan: '"Technical Downtime ORS" akan dihapus permanen.', labelKonfirmasi: "Hapus", destruktif: true }); console.log("jawaban:", ya); }}>uji confirm</button>
```

Periksa dengan mata:

1. Modal muncul, tombol **Batal** yang mendapat fokus awal, bukan tombol Hapus
2. `Esc` menutup dan mengembalikan `false`
3. `Enter` saat fokus di Batal juga mengembalikan `false`
4. Tombol Hapus berwarna `cimoryRed`, dan `console.log` menunjukkan `true`
5. Nama objeknya tampil di pesan, sehingga jelas apa yang akan dihapus

Hapus kode percobaan setelah selesai.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/ConfirmProvider.jsx frontend/src/main.jsx
git commit -m "feat(ui): add a styled confirm dialog that returns a promise"
```

---

### Task 4: Ganti 27 alert() menjadi toast

**Files:**
- Modify: `frontend/src/App.jsx` (4)
- Modify: `frontend/src/components/AddUserModal.jsx` (2)
- Modify: `frontend/src/components/ChangePasswordModal.jsx` (6)
- Modify: `frontend/src/components/LandingPageManager.jsx` (3)
- Modify: `frontend/src/components/Login.jsx` (3)
- Modify: `frontend/src/components/ManageUsers.jsx` (4)
- Modify: `frontend/src/components/NotificationPage.jsx` (1)
- Modify: `frontend/src/components/Register.jsx` (4)
- Modify: `frontend/tests/text-constraints.test.mjs` (turunkan batas)

**Interfaces:**
- Consumes: `useToast()` dari Task 2

**Pemetaan jenis.** Pilih varian dari maksud pesannya, bukan dari tempatnya:

| Maksud pesan | Varian |
|---|---|
| Operasi berhasil | `success` |
| Permintaan ke server gagal | `error` |
| Isian tidak lengkap atau tidak valid | `warning` |
| Keterangan keadaan, bukan hasil aksi | `info` |

- [ ] **Step 1: Ubah satu berkas terlebih dahulu, sebagai pola**

`frontend/src/components/ChangePasswordModal.jsx` punya 6 pemanggilan dan mencakup semua varian, jadi paling cocok jadi contoh.

Tambahkan impor dan hook:

```js
import { useToast } from "./ToastProvider";
```

Di dalam komponen, sebelum `handleSave`:

```js
  const toast = useToast();
```

Ganti keenam pemanggilan. Teks disesuaikan dengan batasan: tidak ada emoji, tidak ada em dash:

```js
    if (!loggedInUser) {
      toast.error("Sesi tidak ditemukan. Silakan login ulang.");
      return;
    }

    if (!currentPassword) {
      toast.warning("Masukkan password saat ini");
      return;
    }

    if (newPassword.length < 6) {
      toast.warning("Password minimal 6 karakter");
      return;
    }

    if (newPassword !== confirmPassword) {
      toast.warning("Konfirmasi password tidak sama");
      return;
    }
```

dan pada blok `try`:

```js
      toast.success("Password berhasil diperbarui");
```

pada `catch`:

```js
      toast.error(err?.response?.data?.message || "Gagal memperbarui password");
```

- [ ] **Step 2: Verifikasi berkas pertama**

Run: `cd frontend && node tests/text-constraints.test.mjs`
Expected: `FAIL pemanggilan alert(): tinggal 21, tapi batas masih 27. Turunkan SISA_PELANGGARAN.alert menjadi 21.`

Kegagalan ini yang diharapkan: ratchet-nya memberitahu angka barunya. Turunkan `SISA_PELANGGARAN.alert` menjadi 21, jalankan lagi, pastikan `4 passed`.

- [ ] **Step 3: Ubah tujuh berkas sisanya**

Terapkan pola yang sama pada tiap berkas. Daftar periksa, tandai setelah selesai:

- [ ] `src/App.jsx` (4)
- [ ] `src/components/AddUserModal.jsx` (2)
- [ ] `src/components/LandingPageManager.jsx` (3)
- [ ] `src/components/Login.jsx` (3)
- [ ] `src/components/ManageUsers.jsx` (4) — termasuk membuang emoji dari `"❌ Error removing user"`, jadi `toast.error("Gagal menghapus user")`
- [ ] `src/components/NotificationPage.jsx` (1)
- [ ] `src/components/Register.jsx` (4)

Untuk komponen yang **bukan** turunan `ToastProvider`, hook-nya tidak akan bekerja. Semua berkas di atas dirender di dalam `<App />`, jadi aman. Bila `useToast` melempar "dipakai di luar ToastProvider" saat dijalankan, berarti komponen itu dirender di luar akar dan harus dilaporkan, bukan diakali dengan menghapus pemeriksaan di provider.

- [ ] **Step 4: Turunkan batas ke nol dan verifikasi**

Run: `cd frontend && node tests/text-constraints.test.mjs`

Expected: melaporkan `tinggal 0, tapi batas masih 21`. Ubah `SISA_PELANGGARAN.alert` menjadi `0`, jalankan lagi.

Expected: `PASS pemanggilan alert(): 0 (sesuai batas)`

- [ ] **Step 5: Verifikasi build dan impor**

Run:

```bash
cd frontend && npx vite build 2>&1 | grep -E "✓ built|error"
```

Expected: `✓ built`

Lalu periksa impor pada setiap berkas yang disentuh:

```bash
cd frontend && node -e "
const fs=require('fs');
const daftar=['src/App.jsx','src/components/AddUserModal.jsx','src/components/ChangePasswordModal.jsx','src/components/LandingPageManager.jsx','src/components/Login.jsx','src/components/ManageUsers.jsx','src/components/NotificationPage.jsx','src/components/Register.jsx'];
for (const f of daftar) {
  const src=fs.readFileSync(f,'utf8');
  if (/useToast\(/.test(src) && !/import\s*\{[^}]*useToast/.test(src)) { console.log('MISSING IMPORT '+f); continue; }
  console.log('OK      '+f);
}"
```

Expected: `OK` untuk kedelapan berkas.

- [ ] **Step 6: Verifikasi manual jalur yang paling sering dipakai**

Jalankan backend dan frontend. Telusuri dan pastikan toast muncul, bukan dialog browser:

1. Login dengan password salah, muncul toast error
2. Ganti password dengan password lama salah, muncul toast error berisi pesan dari server
3. Ganti password dengan benar, muncul toast sukses
4. Add User dengan kolom kosong, muncul toast peringatan
5. Register dengan username yang sudah ada, muncul toast error

Tidak ada satu pun yang boleh memunculkan dialog bawaan browser.

- [ ] **Step 7: Commit**

```bash
git add frontend/src frontend/tests/text-constraints.test.mjs
git commit -m "refactor(ui): replace all 27 browser alerts with toasts"
```

---

### Task 5: Ganti 6 window.confirm() menjadi modal

**Files:**
- Modify: `frontend/src/components/AISettingsModal.jsx` (2)
- Modify: `frontend/src/components/AskAIPanel.jsx` (1)
- Modify: `frontend/src/components/DashboardManager.jsx` (1)
- Modify: `frontend/src/components/LandingPageManager.jsx` (1)
- Modify: `frontend/src/components/ManageUsers.jsx` (1)
- Modify: `frontend/tests/text-constraints.test.mjs`

**Interfaces:**
- Consumes: `useConfirm()` dari Task 3

- [ ] **Step 1: Ubah keenam pemanggilan**

Setiap fungsi pemanggil harus `async`. Bila belum, tambahkan `async` pada tanda tangannya.

`src/components/ManageUsers.jsx`, yang semula `window.confirm("Are you sure to remove this user?")`. Modal menyebut nama orangnya, karena "this user" tidak memberi tahu siapa:

```js
  const confirm = useConfirm();
```

```js
      const setuju = await confirm({
        judul: "Hapus user",
        pesan: `Akun "${user.nama}" akan dihapus permanen beserta riwayat aksesnya.`,
        labelKonfirmasi: "Hapus",
        destruktif: true,
      });
      if (!setuju) return;
```

`src/components/DashboardManager.jsx`, semula `"Delete this dashboard?"`:

```js
      const setuju = await confirm({
        judul: "Hapus dashboard",
        pesan: `"${d.title}" akan dihapus dari daftar. Laporan Power BI aslinya tidak terpengaruh.`,
        labelKonfirmasi: "Hapus",
        destruktif: true,
      });
      if (!setuju) return;
```

`src/components/LandingPageManager.jsx`, semula `"Delete this portal link?"`:

```js
      const setuju = await confirm({
        judul: "Hapus tautan portal",
        pesan: `"${link.title}" akan hilang dari halaman portal.`,
        labelKonfirmasi: "Hapus",
        destruktif: true,
      });
      if (!setuju) return;
```

`src/components/AISettingsModal.jsx`, dua pemanggilan. Yang pertama semula "Hapus kunci pribadi? CODE AI akan memakai kunci universal.":

```js
      const setuju = await confirm({
        judul: "Hapus kunci pribadi",
        pesan: "Setelah dihapus, CODE AI akan memakai kunci universal yang kuotanya dibagi dengan semua user.",
        labelKonfirmasi: "Hapus",
        destruktif: true,
      });
      if (!setuju) return;
```

Yang kedua semula "Hapus kunci universal? User tanpa kunci pribadi tidak bisa memakai CODE AI.":

```js
      const setuju = await confirm({
        judul: "Hapus kunci universal",
        pesan: "User yang tidak punya kunci pribadi tidak akan bisa memakai CODE AI setelah ini.",
        labelKonfirmasi: "Hapus",
        destruktif: true,
      });
      if (!setuju) return;
```

`src/components/AskAIPanel.jsx`, semula "Hapus riwayat chat CODE AI untuk dashboard ini?":

```js
      const setuju = await confirm({
        judul: "Hapus riwayat chat",
        pesan: "Seluruh percakapan CODE AI untuk dashboard ini akan dihapus.",
        labelKonfirmasi: "Hapus",
        destruktif: true,
      });
      if (!setuju) return;
```

Tambahkan `import { useConfirm } from "./ConfirmProvider";` pada tiap berkas.

Catatan untuk `AskAIPanel.jsx`: berkas ini memakai `forwardRef`. Hook tetap dipanggil di dalam badan komponen seperti biasa; jangan memindahkannya ke luar.

- [ ] **Step 2: Turunkan batas dan verifikasi**

Run: `cd frontend && node tests/text-constraints.test.mjs`
Expected: melaporkan `tinggal 0, tapi batas masih 6`. Ubah `SISA_PELANGGARAN.confirm` menjadi `0`, jalankan lagi.
Expected: `PASS pemanggilan window.confirm(): 0`

- [ ] **Step 3: Verifikasi build**

Run: `cd frontend && npx vite build 2>&1 | grep -E "✓ built|error"`
Expected: `✓ built`

- [ ] **Step 4: Verifikasi manual, termasuk pembatalan**

Untuk **setiap** dari enam tempat, uji dua arah. Yang paling penting: menekan Batal harus benar-benar membatalkan, bukan diam-diam melanjutkan.

1. Manage Users, hapus user, tekan Batal. User **tetap ada** di daftar.
2. Manage Users, hapus user probe yang sengaja dibuat, tekan Hapus. User hilang dan muncul toast.
3. Dashboard Manager, hapus dashboard, tekan Esc. Dashboard tetap ada.
4. Portal Link Manager, hapus tautan, tekan Batal. Tautan tetap ada.
5. Pengaturan CODE AI, hapus kunci pribadi, tekan Batal. Kunci tetap ada.
6. Panel CODE AI, hapus riwayat chat, tekan Batal. Riwayat tetap ada.

Untuk nomor 2, buat dulu user sekali pakai lewat Add User. **Jangan memakai akun nyata untuk menguji penghapusan.**

- [ ] **Step 5: Commit**

```bash
git add frontend/src frontend/tests/text-constraints.test.mjs
git commit -m "refactor(ui): replace browser confirms with a dialog that names the target"
```

---

### Task 6: Halaman Notifications

**Files:**
- Modify: `frontend/src/components/NotificationPage.jsx`

**Interfaces:**
- Consumes: `useToast()` dari Task 2

- [ ] **Step 1: Baca struktur yang ada**

Run: `cd frontend && cat src/components/NotificationPage.jsx`

Perhatikan tiga hal sebelum mengubah: bentuk `requests` (`{ userRequests, accessRequests }`), bentuk `userRequests` untuk user biasa, dan fungsi yang menangani approve dan decline.

- [ ] **Step 2: Tambahkan status memproses per baris**

Satu klik ganda pada Approve mengirim dua keputusan. Tambahkan state penanda:

```js
  const [memproses, setMemproses] = useState({});   // kunci -> true
```

Bungkus tiap penangan keputusan:

```js
  const putuskan = async (kunci, aksi) => {
    if (memproses[kunci]) return;
    setMemproses((s) => ({ ...s, [kunci]: true }));
    try {
      await aksi();
      await fetchRequests();
    } catch (err) {
      toast.error(err?.response?.data?.message || "Gagal menyimpan keputusan");
    } finally {
      setMemproses((s) => {
        const salinan = { ...s };
        delete salinan[kunci];
        return salinan;
      });
    }
  };
```

Setiap tombol Approve dan Decline memakai `disabled={Boolean(memproses[kunci])}` dan menampilkan `<Loader2 className="animate-spin" size={14} />` saat memproses. `Loader2` sudah diimpor di berkas ini.

- [ ] **Step 3: Tambahkan skeleton saat memuat**

Ganti penanda memuat yang ada dengan tiga baris kerangka, sehingga tinggi halaman tidak melompat saat data masuk:

```jsx
  if (loading) {
    return (
      <div className="space-y-3" aria-busy="true" aria-label="Memuat notifikasi">
        {[0, 1, 2].map((i) => (
          <div key={i} className="animate-pulse flex items-center gap-3 p-4 bg-white rounded-xl border border-cimoryGray">
            <div className="h-9 w-9 rounded-full bg-gray-200" />
            <div className="flex-1 space-y-2">
              <div className="h-3.5 bg-gray-200 rounded w-1/3" />
              <div className="h-3 bg-gray-100 rounded w-1/2" />
            </div>
            <div className="h-8 w-20 bg-gray-100 rounded-lg" />
          </div>
        ))}
      </div>
    );
  }
```

- [ ] **Step 4: Tambahkan status kosong**

Untuk admin, ketika `userRequests` dan `accessRequests` keduanya kosong:

```jsx
      <div className="text-center py-12">
        <Inbox size={40} className="mx-auto text-gray-300" />
        <p className="mt-3 text-sm font-medium text-gray-700">Belum ada permintaan</p>
        <p className="mt-1 text-xs text-gray-500">
          Permintaan akun baru dan permintaan akses dashboard akan muncul di sini.
        </p>
      </div>
```

Untuk user biasa dengan `userRequests` kosong:

```jsx
      <div className="text-center py-12">
        <Inbox size={40} className="mx-auto text-gray-300" />
        <p className="mt-3 text-sm font-medium text-gray-700">Belum ada permintaan akses</p>
        <p className="mt-1 text-xs text-gray-500">
          Ajukan akses dari kartu dashboard yang belum terbuka, statusnya akan tampil di sini.
        </p>
      </div>
```

Tambahkan `Inbox` ke impor `lucide-react` di berkas ini. `Inbox` dipilih karena artinya memang "kotak masuk yang kosong", bukan sekadar ikon yang tersedia.

- [ ] **Step 5: Pisahkan dua jenis permintaan**

Untuk admin, beri dua judul bagian dan tampilkan jumlahnya, agar tidak lagi bercampur dalam satu daftar:

```jsx
      {requests.userRequests.length > 0 && (
        <section className="mb-8">
          <h2 className="text-sm font-semibold text-cimoryBlue mb-3">
            Permintaan akun baru ({requests.userRequests.length})
          </h2>
          {/* daftar userRequests yang sudah ada, tidak diubah strukturnya */}
        </section>
      )}

      {requests.accessRequests.length > 0 && (
        <section>
          <h2 className="text-sm font-semibold text-cimoryBlue mb-3">
            Permintaan akses dashboard ({requests.accessRequests.length})
          </h2>
          {/* daftar accessRequests yang sudah ada */}
        </section>
      )}
```

- [ ] **Step 6: Verifikasi build dan manual**

Run: `cd frontend && npx vite build 2>&1 | grep -E "✓ built|error"`
Expected: `✓ built`

Verifikasi manual:

1. Login sebagai user biasa yang belum pernah meminta akses, buka Notifications. Muncul status kosong, bukan halaman putih.
2. Muat ulang halaman dan perhatikan sekejap pertama. Muncul skeleton, bukan lompatan tata letak.
3. Login sebagai admin dengan minimal satu permintaan tertunda. Dua bagian terpisah dengan jumlahnya.
4. Klik Approve dua kali cepat. Hanya satu keputusan terkirim, tombol nonaktif setelah klik pertama.

- [ ] **Step 7: Commit**

```bash
git add frontend/src/components/NotificationPage.jsx
git commit -m "feat(ui): give Notifications real empty, loading, and busy states"
```

---

### Task 7: Badge notifikasi dan aksesibilitas sidebar

**Files:**
- Modify: `frontend/src/components/Sidebar.jsx`

**Interfaces:**
- Consumes: `GET /api/notifications/count/:userId` yang sudah ada, klasifikasi `selfOrAdmin`

- [ ] **Step 1: Ambil jumlah yang belum dibaca**

Endpoint ini sudah ada tetapi belum dipakai sidebar. Tambahkan di `Sidebar.jsx`:

```js
  const [jumlahBaru, setJumlahBaru] = useState(0);

  useEffect(() => {
    if (!user?.id) return;
    let dibatalkan = false;

    API.get(`/api/notifications/count/${user.id}`)
      .then((res) => { if (!dibatalkan) setJumlahBaru(Number(res.data?.count) || 0); })
      // Diam saja: badge yang gagal dimuat tidak boleh mengganggu navigasi.
      .catch(() => {});

    return () => { dibatalkan = true; };
  }, [user?.id]);
```

Tambahkan `import API from "../api/api";` bila belum ada di berkas ini, dan `useState`/`useEffect` pada impor React.

Periksa dulu bentuk balasan endpoint-nya sebelum mempercayai `res.data.count`:

```bash
cd backend && grep -n "notifications/count" -A 20 src/server.js | grep -E "res.json|SELECT|count"
```

Sesuaikan `Number(res.data?.count)` dengan nama field yang sebenarnya dikirim.

- [ ] **Step 2: Tampilkan badge**

Pada tombol Notifications, di sebelah label:

```jsx
          <Bell size={18} />
          <span className="flex-1 text-left">Notifications</span>
          {jumlahBaru > 0 && (
            <span
              className="min-w-[1.25rem] px-1.5 py-0.5 rounded-full bg-cimoryRed text-white text-[11px] font-semibold leading-none flex items-center justify-center"
              aria-label={`${jumlahBaru} notifikasi belum dibaca`}
            >
              {jumlahBaru > 9 ? "9+" : jumlahBaru}
            </span>
          )}
```

`"9+"` menjaga lebar badge tetap sehingga tata letak tombol tidak melompat saat angkanya dua digit.

- [ ] **Step 3: Esc menutup sidebar mobile**

```js
  useEffect(() => {
    if (!isOpen) return;
    const onKey = (e) => { if (e.key === "Escape") onClose?.(); };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, [isOpen, onClose]);
```

- [ ] **Step 4: Tandai keadaan akordeon**

Pada tombol baris departemen yang bisa dibuka, tambahkan:

```jsx
                aria-expanded={isExpanded}
                aria-controls={`dash-list-${name}`}
```

dan pada daftar sub-dashboard-nya: `id={`dash-list-${name}`}`.

- [ ] **Step 5: Verifikasi build dan manual**

Run: `cd frontend && npx vite build 2>&1 | grep -E "✓ built|error"`
Expected: `✓ built`

Verifikasi manual:

1. Login sebagai admin dengan permintaan tertunda. Badge angka muncul di tombol Notifications.
2. Setujui semua permintaan, muat ulang. Badge hilang.
3. Perkecil jendela sampai sidebar menjadi drawer, buka, tekan `Esc`. Sidebar tertutup.
4. Tekan `Tab` berulang. Setiap tombol departemen menunjukkan cincin fokus yang terlihat.

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/Sidebar.jsx
git commit -m "feat(ui): show unread count on the sidebar and close it with Esc"
```

---

### Task 8: Hapus framer-motion dari Sidebar

**Files:**
- Modify: `frontend/src/components/Sidebar.jsx`

- [ ] **Step 1: Ganti hover dan tap pada tombol**

Dua `motion.button` dengan `variants={expandBtn} initial="rest" whileHover="hover" whileTap="tap"` menjadi `button` biasa. Tambahkan ke `className` yang sudah ada:

```
hover:scale-[1.02] active:scale-[0.98] motion-reduce:transform-none
```

`motion-reduce:transform-none` menghormati setelan sistem "kurangi gerakan"; framer-motion tidak melakukannya di sini.

Hapus juga konstanta `expandBtn` bila tidak lagi dipakai.

- [ ] **Step 2: Ganti hover pada baris departemen**

`motion.div` dengan `whileHover={allowed ? { scale: 1.02 } : {}}` menjadi `div` biasa dengan kelas kondisional:

```jsx
              <div
                className={`... ${allowed ? "hover:scale-[1.02] active:scale-[0.98] motion-reduce:transform-none" : ""}`}
```

Pertahankan seluruh kelas yang sudah ada; hanya tambahkan.

- [ ] **Step 3: Ganti akordeon dengan grid-template-rows**

Ini satu-satunya bagian yang tidak sepele: CSS tidak bisa mentransisikan `height` ke `auto`. Trik `grid-template-rows` dari `0fr` ke `1fr` menyelesaikannya tanpa mengukur tinggi lewat JavaScript.

Ganti seluruh blok `<AnimatePresence initial={false}>...</AnimatePresence>`:

```jsx
              {/* Sub-daftar dashboard. grid-template-rows 0fr -> 1fr karena CSS
                  tidak bisa mentransisikan height ke auto, dan mengukur tinggi
                  lewat JavaScript berarti memasang kembali yang baru saja
                  dihapus bersama framer-motion. */}
              {hasDashes && (
                <div
                  id={`dash-list-${name}`}
                  className={`grid transition-[grid-template-rows,opacity] duration-200 ease-in-out motion-reduce:transition-none ${
                    isExpanded ? "grid-rows-[1fr] opacity-100" : "grid-rows-[0fr] opacity-0"
                  }`}
                >
                  <ul className="overflow-hidden ml-3 mt-0.5 space-y-0.5 border-l border-white/20 pl-3">
                    {deptDashes.map((dash, idx) => (
                      <li
                        key={idx}
                        onClick={() => handleDashClick(dash)}
                        className="flex items-center gap-2 px-3 py-2 rounded-lg text-xs text-white/85
                          hover:bg-white/20 hover:text-white cursor-pointer transition-all duration-150"
                      >
                        <LayoutGrid size={12} className="shrink-0 text-white/50" />
                        <span className="truncate">{dash.title}</span>
                      </li>
                    ))}
                  </ul>
                </div>
              )}
```

`overflow-hidden` wajib ada pada `ul`, bukan pada `div` pembungkusnya. Di sinilah kesalahan paling mudah terjadi: tanpa itu isi daftar tetap terlihat saat baris grid-nya `0fr`.

Karena elemennya sekarang selalu terpasang, sub-daftar yang tertutup tetap bisa dijangkau `Tab`. Tambahkan pada `ul`: `aria-hidden={!isExpanded}` dan `inert={!isExpanded ? "" : undefined}`.

- [ ] **Step 4: Ganti overlay dan drawer mobile**

`AnimatePresence` dengan `motion.div` backdrop dan `motion.aside` yang meluncur, menjadi elemen yang selalu terpasang dengan transisi CSS. Selalu terpasang diperlukan agar transisi keluar tetap terlihat:

```jsx
      {/* Backdrop drawer mobile */}
      <div
        className={`lg:hidden fixed inset-0 bg-black/50 z-40 transition-opacity duration-200 motion-reduce:transition-none ${
          isOpen ? "opacity-100" : "opacity-0 pointer-events-none"
        }`}
        onClick={onClose}
        aria-hidden="true"
      />

      <aside
        className={`lg:hidden fixed top-0 left-0 h-full w-72 bg-cimoryBlue text-white rounded-r-2xl shadow-2xl z-50 flex flex-col
          transition-transform duration-300 ease-out motion-reduce:transition-none ${
            isOpen ? "translate-x-0" : "-translate-x-full"
          }`}
      >
        {/* isi SidebarContent seperti sebelumnya */}
      </aside>
```

`pointer-events-none` pada backdrop saat tertutup itu wajib: tanpanya, lapisan tak terlihat menutupi seluruh halaman dan tidak ada yang bisa diklik.

- [ ] **Step 5: Pastikan tidak ada sisa impor**

Run:

```bash
cd frontend && grep -n "framer-motion\|motion\.\|AnimatePresence\|whileHover\|whileTap" src/components/Sidebar.jsx
```

Expected: tidak ada keluaran.

- [ ] **Step 6: Verifikasi build dan manual**

Run: `cd frontend && npx vite build 2>&1 | grep -E "✓ built|error"`
Expected: `✓ built`

Verifikasi manual, dengan mata, karena inilah bagian yang paling mudah rusak tanpa terdeteksi build:

1. Desktop: klik departemen yang punya dashboard. Daftarnya terbuka **dengan animasi tinggi**, bukan melompat.
2. Klik lagi. Daftarnya tertutup mulus dan isinya **tidak terlihat menyembul** saat tertutup.
3. Arahkan kursor ke tombol Notifications dan baris departemen. Keduanya membesar sedikit.
4. Perkecil jendela sampai drawer, buka. Drawer meluncur dari kiri, backdrop memudar masuk.
5. Tutup. Drawer meluncur keluar, dan setelah tertutup **halaman di belakang tetap bisa diklik**.
6. Dengan sub-daftar tertutup, tekan `Tab` berulang. Fokus **tidak** masuk ke item yang tersembunyi.

- [ ] **Step 7: Commit**

```bash
git add frontend/src/components/Sidebar.jsx
git commit -m "refactor(ui): replace framer-motion in the sidebar with CSS transitions"
```

---

### Task 9: Hapus framer-motion dari LandingPage dan cabut dependensinya

**Files:**
- Modify: `frontend/src/components/LandingPage.jsx`
- Modify: `frontend/tailwind.config.js`
- Modify: `frontend/package.json`

- [ ] **Step 1: Tambahkan keyframes masuk**

Di `frontend/tailwind.config.js`, tambahkan ke `extend.animation` dan `extend.keyframes`:

```js
        "rise-in": "riseIn 850ms ease-out both",
        "slide-in": "slideIn 280ms ease-in-out both",
```

```js
        riseIn: {
          from: { opacity: "0", transform: "translateY(-22px)" },
          to:   { opacity: "1", transform: "translateY(0)" },
        },
        slideIn: {
          from: { opacity: "0", transform: "translateX(24px)" },
          to:   { opacity: "1", transform: "translateX(0)" },
        },
```

- [ ] **Step 2: Ganti animasi masuk hero**

`motion.div` di baris 82 dengan `initial={{ opacity: 0, y: -22 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.85 }}` menjadi `div` biasa dengan `className="... animate-rise-in motion-reduce:animate-none"`.

- [ ] **Step 3: Ganti transisi halaman carousel**

Ini penyederhanaan yang disengaja dan dicatat. `AnimatePresence mode="wait"` menganimasikan halaman yang **keluar** dengan menahannya tetap terpasang; CSS tidak bisa melakukan itu tanpa memasang kembali mekanisme yang baru saja kita hapus. Yang kita pertahankan adalah animasi halaman yang **masuk**.

Ganti blok `AnimatePresence` dan `motion.div` bervariasi `pageVariants` dengan `div` biasa yang di-`key` pada indeks halaman, sehingga React memasangnya ulang dan animasi CSS berjalan setiap ganti halaman:

```jsx
                  <div key={page} className="animate-slide-in motion-reduce:animate-none">
                    {/* isi halaman carousel seperti sebelumnya */}
                  </div>
```

Ganti `page` dengan nama variabel indeks halaman yang sebenarnya dipakai di berkas itu. Periksa dengan:

```bash
cd frontend && grep -n "ITEMS_PER_PAGE\|setPage\|currentPage\|page" src/components/LandingPage.jsx | head -10
```

Hapus konstanta `pageVariants` setelahnya.

- [ ] **Step 4: Ganti hover kartu dan tombol**

`motion.div` dengan `whileHover={{ scale: 1.14, y: -7, filter: "drop-shadow(...)" }}` menjadi `div` biasa:

```
hover:scale-[1.14] hover:-translate-y-[7px] hover:drop-shadow-xl transition-transform duration-200 motion-reduce:transform-none
```

`motion.button` di baris 202 dengan `whileHover={{ y: -3, scale: 1.04 }}` menjadi `button` biasa:

```
hover:-translate-y-[3px] hover:scale-[1.04] transition-transform duration-200 motion-reduce:transform-none
```

- [ ] **Step 5: Pastikan tidak ada sisa, lalu cabut dependensinya**

Run:

```bash
cd frontend && grep -rn "framer-motion\|motion\.\|AnimatePresence\|whileHover\|whileTap" src/
```

Expected: tidak ada keluaran. Bila masih ada, jangan lanjut mencabut paketnya.

Lalu:

```bash
cd frontend && npm uninstall framer-motion
```

- [ ] **Step 6: Verifikasi chunk-nya benar-benar hilang**

Run:

```bash
cd frontend && rm -rf dist && npx vite build 2>&1 | grep -E "✓ built|error" && ls -l dist/assets/*.js | awk '{printf "%8.1f KB  %s\n", $5/1024, $9}' | sort -rn | head -8
```

Expected: `✓ built`, dan **tidak ada** chunk `proxy-*.js` berukuran sekitar 114,5 KB. Chunk itulah framer-motion.

Catat juga JS halaman login:

```bash
cd frontend && node -e "
const fs=require('fs');const html=fs.readFileSync('dist/index.html','utf8');
const n=[...html.matchAll(/(?:src|href)=\"\/assets\/([^\"]+\.js)\"/g)].map(m=>m[1]);
let t=0;for(const x of n)t+=fs.statSync('dist/assets/'+x).size;
console.log((t/1024).toFixed(1)+' KB (sebelum task ini: 242,8 KB)');"
```

- [ ] **Step 7: Verifikasi manual halaman portal**

Buka `/cop`. Periksa dengan mata:

1. Judul dan logo masuk dengan animasi turun-memudar, bukan langsung muncul
2. Klik panah carousel. Halaman berikutnya masuk dengan animasi geser
3. Arahkan kursor ke kartu departemen. Kartu membesar dan terangkat
4. Hero tampil utuh, tidak terpotong, tidak ada lompatan tata letak

Hidupkan "kurangi gerakan" di setelan sistem operasi, muat ulang, dan pastikan animasinya berhenti tanpa merusak tata letak.

- [ ] **Step 8: Commit**

```bash
git add frontend/src/components/LandingPage.jsx frontend/tailwind.config.js frontend/package.json frontend/package-lock.json
git commit -m "refactor(ui): drop framer-motion for CSS and remove the dependency"
```

---

### Task 10: Bersihkan emoji dan em dash yang tersisa

**Files:**
- Modify: `frontend/src/components/DashboardManager.jsx`
- Modify: `frontend/src/components/PerfSummary.jsx`
- Modify: `frontend/src/components/AISettingsModal.jsx`
- Modify: `frontend/tests/text-constraints.test.mjs`

- [ ] **Step 1: Ganti emoji status tabel dengan ikon yang cocok maknanya**

`src/components/DashboardManager.jsx:315` memakai `"✅"` untuk menandakan kolom URL terisi, dan em dash untuk menandakan kosong. Kolomnya berisi URL, jadi ikon yang tepat adalah ikon tautan, bukan tanda centang umum:

```jsx
                          {d.url
                            ? <Link2 size={14} className="text-green-600" aria-label="URL terisi" />
                            : <span className="text-gray-300 text-xs">Belum diisi</span>}
```

Tambahkan `Link2` ke impor `lucide-react` di berkas itu.

- [ ] **Step 2: Ganti em dash penanda kosong di PerfSummary**

`src/components/PerfSummary.jsx:4`:

```js
const ms = (v) => (typeof v === "number" ? `${Math.round(v)} ms` : "belum ada data");
```

- [ ] **Step 3: Buang em dash dari kalimat**

`src/components/DashboardManager.jsx:70`:

```js
  if (!raw) return { level: "empty", message: "Kosong. Export Mode dan CODE AI tidak aktif untuk dashboard ini." };
```

`src/components/DashboardManager.jsx:71`:

```js
  if (GUID_RE.test(raw)) return { level: "ok", message: "Report GUID valid. Export Mode dan CODE AI aktif." };
```

`src/components/AISettingsModal.jsx:140`:

```jsx
                Kamu memakai kunci universal, kuotanya dibagi dengan semua user.
```

`src/components/AISettingsModal.jsx:151`:

```jsx
            {status?.hasUserKey && <span className="text-gray-400 font-normal"> (kosongkan bila tidak diubah)</span>}
```

- [ ] **Step 4: Turunkan kedua batas ke nol**

Run: `cd frontend && node tests/text-constraints.test.mjs`

Expected: melaporkan sisa 0 untuk `emojiUi` dan `emDashUi` sementara batasnya masih di atas nol. Ubah keduanya menjadi `0`, jalankan lagi.

Expected: keempat pemeriksaan `PASS` dengan angka 0.

Setelah semua nol, ubah komentar di atas `SISA_PELANGGARAN`:

```js
// Semua nol. Angka ini tidak boleh naik: uji gagal begitu ada pelanggaran baru.
```

- [ ] **Step 5: Verifikasi build dan manual**

Run: `cd frontend && npx vite build 2>&1 | grep -E "✓ built|error" && npm test 2>&1 | tail -4`
Expected: `✓ built`, lalu kedua berkas uji lulus.

Verifikasi manual:

1. Dashboard Manager: kolom URL menampilkan ikon tautan untuk yang terisi dan teks "Belum diisi" untuk yang kosong. Tidak ada emoji.
2. Manage Users, bagian Performa: metrik tanpa data menampilkan "belum ada data".
3. Pengaturan CODE AI: kalimat kunci universal terbaca wajar tanpa em dash.

- [ ] **Step 6: Commit**

```bash
git add frontend/src frontend/tests/text-constraints.test.mjs
git commit -m "fix(ui): replace emoji icons and em dashes with fitting icons and plain text"
```

---

### Task 11: Verifikasi akhir dan laporan hasil

**Files:**
- Modify: `docs/superpowers/plans/2026-08-03-uiux.md` (isi tabel hasil)

- [ ] **Step 1: Seluruh uji hijau**

Run:

```bash
cd frontend && npm test 2>&1 | tail -6
cd ../backend && timeout 150 npm test 2>&1 | grep -E "FAIL|passed"
```

Expected: frontend kedua berkas lulus; backend 136 passed, 0 failed.

- [ ] **Step 2: Build bersih dan tidak ada sisa**

Run:

```bash
cd frontend && rm -rf dist && npx vite build 2>&1 | grep -E "✓ built|error"
grep -rn "framer-motion" package.json src/ || echo "framer-motion bersih"
grep -rn "alert(\|window.confirm(" src/ || echo "dialog bawaan browser bersih"
```

Expected: `✓ built`, lalu kedua baris "bersih".

- [ ] **Step 3: Ukur hasil akhir**

Run:

```bash
cd frontend && node -e "
const fs=require('fs');const html=fs.readFileSync('dist/index.html','utf8');
const n=[...html.matchAll(/(?:src|href)=\"\/assets\/([^\"]+\.js)\"/g)].map(m=>m[1]);
let t=0;for(const x of n)t+=fs.statSync('dist/assets/'+x).size;
console.log('halaman login: '+(t/1024).toFixed(1)+' KB');"
ls -l dist/assets/*.js | awk '{s+=$5} END {printf "total JS: %.1f KB dalam %d chunk\n", s/1024, NR}'
```

- [ ] **Step 4: Isi tabel hasil**

Isi dengan angka sebenarnya. **Laporkan apa adanya**, termasuk bila ada animasi yang disederhanakan atau target yang tidak tercapai.

| Ukuran | Sebelum | Sesudah | Target | Status |
|---|---|---|---|---|
| `alert()` di `src/` | 27 | **0** | 0 | tercapai |
| `window.confirm()` di `src/` | 6 | **0** | 0 | tercapai |
| Emoji sebagai ikon di antarmuka | **9** | **0** | 0 | tercapai, perkiraan spec 2 |
| Em dash di teks antarmuka | **21** | **0** | 0 | tercapai, perkiraan spec 6 |
| `framer-motion` di `package.json` | ada | tidak ada | tidak ada | tercapai |
| Chunk framer-motion | 114,5 KB | tidak terbentuk | tidak terbentuk | tercapai |
| JS halaman login | 242,8 KB | **248,3 KB** | turun | **tidak tercapai**, naik 5,5 KB |
| Total JS seluruh chunk | 825,6 KB | **750,0 KB** | tidak ditargetkan | turun 75,6 KB |
| Uji frontend | 18 | **22 lulus** | lulus semua | tercapai, +4 uji penjaga |
| Uji backend | 136 | **145 lulus** | 136 lulus | tercapai, +9 uji hitung notifikasi |

### Yang tidak tercapai, dan sebabnya

**JS halaman login naik 5,5 KB, bukan turun.** `ToastProvider` dan
`ConfirmProvider` harus diimpor statis: keduanya wajib sudah ada sebelum
komponen pertama memanggil `useToast()`, jadi tidak bisa di-lazy. Itu harga
untuk menghapus 33 dialog bawaan browser yang memblokir halaman. Perkiraan awal
di rencana bahwa angka ini akan turun keliru, karena penghematan framer-motion
seluruhnya jatuh di jalur setelah-login, bukan di halaman login.

**Angka pelanggaran teks jauh lebih besar dari perkiraan spec.** 9 emoji dan 21
em dash, bukan 2 dan 6. Perkiraan itu berasal dari `grep` yang hanya melihat
string literal, sementara sebagian besar pelanggaran justru berada di teks JSX.

### Penyederhanaan yang disengaja

Animasi halaman yang **keluar** pada carousel LandingPage dihapus, hanya animasi
masuk yang dipertahankan. Menahan elemen keluar tetap terpasang adalah persisnya
pekerjaan `AnimatePresence`, dan membangunnya ulang dengan tangan membatalkan
tujuan mencabut dependensinya.

### Bug yang ditemukan dan diperbaiki di luar rencana

1. **Hitungan notifikasi selalu nol.** `GET /api/notifications/count/:userId`
   memakai `status != last_notified_status`, dan kolom itu NULL sampai user
   membuka halaman notifikasi. Di SQL `status != NULL` menghasilkan NULL, bukan
   true. User yang aksesnya baru disetujui tidak mendapat tanda apa pun.
   Diperbaiki dengan perbandingan null-safe, dijaga uji backend.
2. **`Return;` dengan R kapital** di `ManageUsers.deleteUser`. Menekan Batal saat
   menghapus user melempar `ReferenceError`, bukan membatalkan.
3. **Penjaga klik ganda tidak bekerja** di versi pertama. Lima klik sinkron
   membaca state yang sama dari closure dan mengirim lima keputusan. Terukur.
   Sekarang memakai ref.
4. **Seluruh isi sidebar dibangun ulang setiap render.** `SidebarContent`
   dideklarasikan sebagai arrow component di dalam `Sidebar`, sehingga setiap
   render menghasilkan tipe komponen baru. Transisi CSS tidak akan pernah
   terlihat pada node yang baru dibuat.
5. **Gambar kartu portal rusak** di halaman publik. Seluruh 12 tautan menyimpan
   URL absolut berisi host dan port yang di-hardcode, padahal berkasnya
   disajikan oleh backend yang sama di `/uploads`. Dinormalkan saat dibaca.
6. **Baris departemen tidak bisa dijangkau keyboard.** Sebelumnya `div` dengan
   `onClick`, sekarang `button` sungguhan.
7. **Hapus dashboard yang gagal tidak menampilkan apa pun**, hanya
   `console.error`.
8. **Logo Cimory terunduh dua kali** dengan URL berbeda: versi ber-hash dari
   bundler di LandingPage, dan path runtime di header.

### Temuan yang dilaporkan, tidak diperbaiki

**19 baris `access_requests` yatim** dengan `user_id` menunjuk ke user yang sudah
dihapus, tertua dari Oktober. `DELETE /api/delete-user/:id` tidak membersihkan
tabel itu. Bukan jejak pengujian: id-nya 7, 9, 11, dan 12, sementara akun uji
memakai id 131 ke atas dan seluruhnya sudah dihapus beserta barisnya.

Catat juga di bawah tabel: animasi keluar carousel pada LandingPage dihapus dengan sengaja, hanya animasi masuk yang dipertahankan, karena menahan elemen keluar tetap terpasang berarti memasang kembali mekanisme yang framer-motion sediakan.

- [ ] **Step 5: Commit dan push**

```bash
git add docs/superpowers/plans/2026-08-03-uiux.md
git commit -m "docs(ui): record measured before/after results"
git push -u origin feat/uiux
```
